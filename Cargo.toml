[package]
name = "rust_mixer_test"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "3.0.0-rc.7", features=["derive"] }
cpal = "0.13.4"
rand = { version = "0.8.4", features=["small_rng"] }
